name: test_app
description: RAG + LLM integration test app for mobile_rag_engine

publish_to: none

environment:
  sdk: ^3.9.2
  flutter: ">=3.9.0"

dependencies:
  flutter:
    sdk: flutter
  mobile_rag_engine:
    path: ../
  flutter_gemma: ^0.11.13
  path_provider: ^2.1.5
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/.env
